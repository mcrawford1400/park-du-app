# expo-router remains a transitive dependency of @expo/cli's dev-server tooling
# even though this project doesn't use it (react-navigation is used directly).
# This disables Metro's false-positive compatibility check between the two.
EXPO_ROUTER_DISABLE_RN_NAVIGATION_CHECK=1
